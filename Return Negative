using System;

public static class Kata
{
  public static int MakeNegative(int number)
  {
    //Math.Abs looks better and is easily  readble. 
    if(number > 0) {
      return -Math.Abs(number);
    }
    return number;
  }
}
